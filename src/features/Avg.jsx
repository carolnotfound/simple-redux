import React from "react";
import Card from "../components/Card";

import "./Avg.css";

const Avg = (props) => {
  const result = 5;

  return <Card title="Avg">{result}</Card>;
};

export default Avg;
