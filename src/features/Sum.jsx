import React from "react";
import Card from "../components/Card";

import "./Sum.css";

const Sum = (props) => {
  const result = 5;

  return <Card title="Sum">{result}</Card>;
};

export default Sum;
